# ============================================================
# Annadata OS - Root Python Dependencies (Universal Mode)
# Supports both Docker (Postgres/Redis) and Local (SQLite/No-Docker)
# ============================================================

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
aiosqlite>=0.19.0              # Required for SQLite mode
alembic>=1.13.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.0
bcrypt<4.1                     # passlib 1.7.x is incompatible with bcrypt 4.1+
email-validator>=2.0.0         # required by Pydantic EmailStr

# Caching & Task Queue
redis>=5.0.0
celery>=5.3.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0

# Rate Limiting
slowapi>=0.1.9

# Data / ML
pandas>=2.1.0
numpy>=1.24.0
scikit-learn>=1.3.0

# AI & Serial
openai>=1.0.0
pyserial>=3.5
